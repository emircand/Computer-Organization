module alu (
    input a,
    input b,
    input less,
    input cin,
    input [2:0] aluop,
    output cout,
    output r
);

xor xor_out(b, alu[2]);
or or_out(xor_out, a);
and and_out(xor_out, a);
inv inv_and_out(and_out);

    
endmodule